# Отключение пресетов (No Auto SL/TP)

## 📋 Описание

Добавлена возможность отключать автоматические стоп-лоссы и тейк-профиты с помощью знака `-` (дефис) или `—` (тире) в конце команды.

## 🎯 Использование

### Синтаксис

```
l|s <монета> <usd> <цена> -
l|s <монета> <usd> -
```

### Примеры

#### Отложенный вход без SL/TP
```
l hype 200 3.5 -
```
**Результат:**
- ✅ Выставляется LIMIT или STOP ордер на вход
- ❌ НЕ ставится автоматический стоп-лосс
- ❌ НЕ ставятся автоматические тейк-профиты
- 📊 Позиция остаётся открытой без защиты

#### Market вход без SL/TP
```
l eth 1000 -
```
**Результат:**
- ✅ Мгновенный вход по рынку
- ❌ Без автоматического SL
- ❌ Без автоматических TP

#### Множественные входы без SL/TP
```
l xrp 500 2.35 300 2.33 200 2.31 -
```
**Результат:**
- ✅ Выставляются 3 отложенных ордера
- ❌ Без автоматических SL/TP

## 💡 Зачем это нужно?

### Кейсы использования:

1. **Ручное управление позицией**
   - Вы хотите самостоятельно контролировать выход
   - Используете собственную стратегию

2. **Тестирование входов**
   - Проверка качества входа без риск-менеджмента
   - Эксперименты с ценами

3. **Долгосрочные позиции**
   - HODL стратегии
   - Позиции без фиксированного SL

4. **Арбитраж/скальпинг**
   - Быстрые входы/выходы
   - Ручной контроль

## ⚠️ Важные моменты

### Безопасность
- ⚠️ **Без SL позиция не защищена от больших убытков**
- ⚠️ **Следите за позицией вручную**
- ⚠️ **Рекомендуется использовать только опытным трейдерам**

### Что НЕ отключается
- ✅ Входные ордера выставляются как обычно
- ✅ Можно вручную закрыть через `close <symbol>`
- ✅ Можно вручную установить SL/TP через биржевой интерфейс

## 📊 Примеры команд

### С пресетом (обычный режим)
```bash
# С автоматическими SL и TP
l xrp 500 2.35 4h

# Результат:
# - Вход LIMIT @ 2.35
# - SL автоматически
# - TP на 3R, 5R, 7R
```

### Без пресета (новый режим)
```bash
# Только вход, без SL/TP
l xrp 500 2.35 -

# Результат:
# - Вход LIMIT @ 2.35
# - Без SL
# - Без TP
```

## 🔧 Технические детали

### Изменённые файлы

1. **src/core/types.ts**
   - Добавлен флаг `noPreset?: boolean` в тип `ParsedCmd`

2. **src/core/CommandParser.ts**
   - Парсер распознаёт `-` или `—` как `noPreset = true`
   - Работает для обоих режимов: LIMIT и MARKET

3. **src/core/engine.ts**
   - Логика установки SL обёрнута в `if (!noPreset)`
   - Логика установки TP обёрнута в `if (!noPreset)`
   - Применяется для MARKET и LIMIT/STOP входов

4. **src/core/HelpText.ts**
   - Добавлены примеры использования `-`
   - Добавлена подсказка в раздел "ПОДСКАЗКИ"

### Логика работы

```typescript
// Парсинг команды
if (tok === "-" || tok === "—") {
  noPreset = true;
}

// В engine.ts
if (!noPreset) {
  // Устанавливаем SL
  await ex.createStopMarketClose(...);
  
  // Устанавливаем TP
  await ex.createReduceOnlyLimit(...);
} else {
  // Просто сообщаем о входе
  info("✅ Вход выполнен без SL/TP");
}
```

## 🧪 Тестирование

### Тест 1: LIMIT вход без SL/TP
```bash
# 1. Отправить команду
l xrp 500 2.30 -

# 2. Проверить что:
#    - Выставлен LIMIT ордер
#    - НЕТ стоп-лосса
#    - НЕТ тейк-профитов

# 3. Проверить через команду orders
orders xrp

# Должен быть только 1 LIMIT ордер на вход
```

### Тест 2: MARKET вход без SL/TP
```bash
# 1. Отправить команду
l eth 1000 -

# 2. Дождаться исполнения

# 3. Проверить через команду orders
orders eth

# Не должно быть SL и TP ордеров
```

### Тест 3: С пресетом (проверка что не сломали)
```bash
# 1. Отправить команду БЕЗ дефиса
l xrp 500 2.35 4h

# 2. Проверить что:
#    - Выставлен LIMIT ордер
#    - ЕСТЬ стоп-лосс
#    - ЕСТЬ тейк-профиты

# 3. Проверить через команду orders
orders xrp

# Должны быть: 1 LIMIT вход + 1 STOP (SL) + несколько LIMIT (TP)
```

## 📝 Changelog

### 2025-11-28
- ✅ Добавлена поддержка `-` для отключения пресетов
- ✅ Работает для LIMIT и MARKET входов
- ✅ Обновлён Help с примерами
- ✅ Полная обратная совместимость
- ✅ 0 ошибок линтера

## ⚡ FAQ

**Q: Можно ли вручную установить SL после входа?**  
A: Да, через биржевой интерфейс Binance или торговый терминал.

**Q: Как закрыть позицию без SL?**  
A: Используйте команду `close <symbol>` или закройте вручную через биржу.

**Q: Работает ли `-` с переопределением риска?**  
A: Да, можно комбинировать: `50 l xrp 500 2.35 -` (риск $50, но без SL/TP)

**Q: Можно ли комбинировать с --dry?**  
A: Да: `l xrp 500 2.35 - --dry` (превью без SL/TP)

**Q: Что если ошибочно добавил дефис?**  
A: Можно отменить через Cancel кнопку в уведомлении (если уведомления включены)

**Q: Влияет ли на существующий функционал?**  
A: Нет, полная обратная совместимость. Без `-` всё работает как раньше.

## ✅ Готово к использованию!

Функция протестирована и готова к продакшену.

**Используйте осторожно! Без SL позиция не защищена! ⚠️**

